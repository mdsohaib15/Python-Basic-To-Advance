# dir function used to print all name of function that are avaiable in the module
import math
print(dir(math))

# check the keyword what are actually they are like variable,function or float
print(math.pi,type(math.pi))
print(math.sqrt,type(math.sqrt))